# מדריך פריסה - שליחת מייל דרך Google Apps Script

מדריך זה מסביר איך לפרוס את `Code.gs` כ-Web App שישלח את המייל, ואיך
לחבר אותו למערכת ב-Vercel.

## שלב 1: יצירת פרויקט Apps Script

1. גשו ל-<https://script.google.com> והתחברו עם חשבון ה-Gmail שממנו
   *בפועל* יישלחו המיילים (זו מגבלה של Google: GmailApp שולח תמיד
   מהחשבון שמריץ את ה-Script).
2. "פרויקט חדש" (New project).
3. מחקו את התוכן שבקובץ `Code.gs` שנוצר כברירת מחדל, והדביקו במקומו
   את כל התוכן של הקובץ `Code.gs` המצורף כאן.
4. שמרו את הפרויקט (למשל בשם "מחשבון מחמאות - שליחת מייל").

## שלב 2: הגדרת הסוד המשותף (Script Properties)

הסוד המשותף מגן על ה-Web App מפני שימוש לא מורשה על ידי גורם חיצוני.

1. בתפריט הצד: **Project Settings** (סמל גלגל השיניים).
2. גללו למטה ל-**Script Properties** → **Add script property**.
3. Property: `MAIL_SHARED_SECRET`
   Value: מחרוזת אקראית וסודית משלכם (למשל 32 תווים אקראיים).
   **שמרו את הערך הזה בצד** - תצטרכו אותו גם ב-Vercel בשלב 4.
4. שמרו.

## שלב 3: פריסה כ-Web App

1. בפינה הימנית העליונה: **Deploy** → **New deployment**.
2. לחצו על סמל הגלגל שיניים לצד "Select type" ובחרו **Web app**.
3. הגדרות הפריסה:
   - **Execute as:** `Me` (אתם - כך המייל יישלח מכתובת ה-Gmail שלכם)
   - **Who has access:** `Anyone` (חובה - אחרת Vercel לא יוכל לקרוא
     ל-Web App ותקבלו דף HTML של Google במקום JSON, בדיוק כמו הבעיה
     שהייתה עם התמלול)
4. לחצו **Deploy**.
5. Google יבקש הרשאות (Authorize access) - אשרו עם חשבון ה-Gmail שלכם.
6. תקבלו **Web app URL** בפורמט:
   `https://script.google.com/macros/s/AKfycb.../exec`
   **העתיקו את הכתובת הזו** - היא ה-`GOOGLE_SCRIPT_URL`.

> אם בעתיד תעדכנו את הקוד ב-`Code.gs`, יש לבצע **New deployment** נוסף
> (או Manage deployments → עריכת הפריסה הקיימת) כדי שהשינויים ייכנסו
> לתוקף ב-URL הקיים.

## שלב 4: הגדרת משתני הסביבה ב-Vercel

בפרויקט ב-Vercel: **Settings** → **Environment Variables**, הוסיפו:

| שם המשתנה | ערך |
|---|---|
| `GOOGLE_SCRIPT_URL` | כתובת ה-Web App שהתקבלה בשלב 3.6 |
| `MAIL_SHARED_SECRET` | אותה מחרוזת סודית שהוגדרה בשלב 2.3 |

לאחר ההוספה, בצעו **Redeploy** לפרויקט (משתני סביבה נטענים רק בבנייה
מחדש).

## שלב 5: בדיקה

1. התקשרו לשלוחה, עברו את הזרימה עד לתפריט התוצאות, ובחרו באפשרות
   שליחת התוצאות למייל (הקישה 3 בתפריט שאחרי תוצאה).
2. הקלידו כתובת מייל באמצעות מקלדת ההקלדה של ימות (EmailKeyboard).
3. בדקו שהמייל התקבל, עם שם השולח המוצג **"מערכת מחשבון מחמאות"**
   וקובץ TXT מצורף עם כל התוצאות.
4. אם משהו נכשל - בדקו את הלוגים:
   - ב-Vercel: **Deployments** → הפרויקט → **Logs** (חפשו
     `EMAIL_SEND_REQUEST` / `EMAIL_SEND_RESPONSE` / `EMAIL_SENT_FAILED`).
   - ב-Google Apps Script: **Executions** (בתפריט הצד) - מציג כל הרצה
     של `doPost` ואם הייתה בה שגיאה.

## תקלות נפוצות

- **תגובה שהיא HTML ולא JSON** ("Unexpected token '<'..."): כמעט
  בוודאות "Who has access" לא הוגדר כ-`Anyone` בפריסה (שלב 3), או
  שנעשה שינוי בקוד בלי לפרוס Deployment חדש.
- **"Unauthorized: secret לא תואם"**: `MAIL_SHARED_SECRET` ב-Vercel
  לא תואם למה שהוגדר ב-Script Properties (שלב 2) - ודאו שאין רווחים
  מיותרים בהעתקה/הדבקה.
- **המייל לא מגיע בכלל (אין שגיאה)**: בדקו תיקיית קידומים/ספאם -
  מיילים שנשלחים דרך GmailApp עם שם תצוגה שונה משם החשבון עלולים
  להיתפס כחשודים על ידי חלק משרתי הדואר של הנמען.
